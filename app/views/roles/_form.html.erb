<%= form_for(@role) do |f| %>
  <% if @role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
      <% @role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>

    </div>
  <% end %>

    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>

    <%= render 'permissions' %>

    <!--
    <div class="field">
      <%= check_box_tag(:person_read_only) %>
      <%= label_tag(:person_read_only, "Person_Read_Only") %>
      <%= f.label :permissions %>
      <%= f.number_field :permissions %>
    </div>
    -->

  <div class="actions">
    <%= f.submit :class => 'button tiny' %>
  </div>

<% end %>

