<%= form_for(@inventory_order_item) do |f| %>
  <% if @inventory_order_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inventory_order_item.errors.count, "error") %> prohibited this inventory_order_item from being saved:</h2>

      <ul>
      <% @inventory_order_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :orderid %><br>
    <%= f.number_field :orderid %>
  </div>
  <div class="field">
    <%= f.label :itemid %><br>
    <%= f.number_field :itemid %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
